# .env.example — repo 根 .env 範本（019-secrets-sops T024；FR-017／contracts P5.1）
# .env 本體 gitignored（.gitignore 既有 `.env` 規則已覆蓋、無另加行）、由 `bash tools/bootstrap`
# 代勞產生；本檔僅示範形制，不寫死本機路徑（per-machine 值不入版控）。
#
# ★產檔約束（compose 與 deploy/ 四腳本〔decrypt／generate／preflight／setup-reaper-role〕
#   共同前提，違反即被腳本側嚴格解析 FAIL 拒用）：
#   1. 單行 KEY=VALUE（無 export、無引號、無行內註解）。
#   2. ★docker compose 讀 .env 時「不做 shell 展開」——值裡寫 $HOME 只是三個字面字元、
#      不會變成家目錄；SECRETS_DIR 必須是「絕對路徑字面」（bootstrap 於 shell 側展開後寫入）。
#   3. 值不得含空白與 shell 元字元（腳本只嚴格解析 SECRETS_DIR 一行、不整檔 source——
#      compose 的 .env 允許不加引號的含空白值、井號語意亦與 shell 不同，整檔 source 時
#      含錢字號小括號或反引號之值會被執行）。
#
# SECRETS_DIR：解密後明文機密檔落點（單一事實來源；拍板值＝ADR 0080 決策 2：
#   $HOME/.cache/rev4-secrets 之絕對路徑字面、ext4 持久、免開機儀式）。
#   消費者＝docker-compose.yml 頂層 secrets 段（compose 原生讀 .env）＋上列四腳本。
#   未設時一律回退 repo 內 ./deploy/secrets（向後相容；忘設＝保護失效，由 preflight 與
#   bootstrap 斷言補償、ADR 誠實登記）。
# SECRETS_DIR=/home/USERNAME/.cache/rev4-secrets
#
# SOCKET_PROXY_GID：obs profile 之 socket-proxy 所需 docker.sock gid（選填；用途與查法
#   見 docs/ops/RUNBOOK.md §4；不設＝退 compose 預設 1001）。
# SOCKET_PROXY_GID=1001
