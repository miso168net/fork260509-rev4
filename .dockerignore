# Workspace-root .dockerignore
# Compose build context = ./ (workspace root). 沒這檔 Docker 會把整個 workspace
# (含 fork 源倉 / base-web worktree / graphify-out / .git) 串進 build daemon, 數 GB 等級慢。
# 只留 Dockerfile 真正需要的: rust-api/ + deploy/

# fork 源倉 (worktree 源頭, 本機留著但 image 不需要)
# 注意: fork260509-rev2-anew-rust-api 是 GitHub repo 永久名(不隨 rev3 改名)
fork260509-soybean-admin-base/
fork260509-soybean-admin-docs/
fork260509-rev2-anew-rust-api/

# 另一個 worktree (rust-api build 不需要 base-web;prod base-web build context=./base-web 不受此檔影響)
base-web/

# graphify 輸出 (graph.html 3MB+ / obsidian 5000+ markdown)
graphify-out/

# Git internals
.git/
**/.git
**/node_modules/

# Rust artifacts (任何 worktree 的 target/)
**/target/

# Docs / specs / tests (不會 COPY 進 image, 加速 tar)
docs/
specs/
tests/

# Claude / spec-kit / IDE state
.claude/
.specify/
.vscode/
.idea/

# Logs / caches
**/*.log
**/.cache/
**/__pycache__/

# 機密與私鑰 (Dockerfile 不 COPY 這兩目錄;compose secrets file: 由 host 直讀、不走 context)
# 防 remote builder / CI 情境下真值 secrets 與 dev 私鑰被 tar 進 build daemon (T010 review I-1)
deploy/secrets/
deploy/dev-certs/
