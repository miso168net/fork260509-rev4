# ============================================================
# graphify 排除清單
# 語法：與 .gitignore 同,支援 ! 反向取消
# ============================================================


# ============================================================
# worktree vs 源倉:擇一索引,避免雙倍索引 / node ID 碰撞
# 理由:base-web/ 與 fork260509-soybean-admin-base/ 透過 git worktree 共用 .git(rust 同理),
#      內容高度重疊(101/102 .ts 同相對路徑 → 撞 node ID)。
#      改為索引 worktree(rev3 整合分支 = 真實碼),排除對應源倉(example/main 分支)。
#      docs 源倉 fork260509-soybean-admin-docs/ 非 worktree、純參考;2026-06-24 改為【排除】
#      (與整合碼無關、~1856 noise 節點〔多語 nodejs/use-table/FAQ/structure 文件〕蓋過程式碼真節點、
#       prune 出圖;CLAUDE.md §8.3 本意=只索引 base-web/rust-api worktree 真實碼)。
# ============================================================
fork260509-soybean-admin-base/
fork260509-rev2-anew-rust-api/
fork260509-soybean-admin-docs/

# ============================================================
# graphify 自己的輸出
# 避免把上次的 GRAPH_REPORT.md / obsidian/ 當輸入再算一次
# ============================================================
graphify-out/
specs/
docs/

# ============================================================
# 環境 / 暫存
# ============================================================
target/
.specify/
.claude/
CLAUDE.md
README.md

# ============================================================
# Lock files（純資料、無語意可抽,且檔案非常大）
# ============================================================
**/pnpm-lock.yaml
**/Cargo.lock
**/package-lock.json



# ============================================================
# Secrets / env
# ============================================================
.env
.env.local
.env.*.local
deploy/.env
*.pem
*.key
.dev.vars

# ============================================================
# Docker / dev container 個人 override
# ============================================================
deploy/*
docker-compose.*.yml
.devcontainer/devcontainer.local.json

# ============================================================
# Node / pnpm / TypeScript
# ============================================================
node_modules/
.pnpm-store/
dist/
coverage/
.vitest-cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ============================================================
# Python（graphify 工具暫存）
# ============================================================
__pycache__/
*.pyc
*.pyo
*.pyd

# ============================================================
# IDE
# ============================================================
.vscode/*
#!.vscode/extensions.json
#!.vscode/settings.shared.json
.idea/
*.swp
*.swo
*~

# ============================================================
# OS
# ============================================================
.DS_Store
Thumbs.db
desktop.ini

# ============================================================
# Logs / 暫存 / 殘骸
# ============================================================
*.log
logs/
*.tmp
*.bak
.cache/
*.orig
*.rej
tests/
